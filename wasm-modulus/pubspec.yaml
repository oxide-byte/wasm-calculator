# pubspec.yaml
name: dart_modulus
description: A Dart package that calculates modulus, compiled to WebAssembly
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  js: ^0.6.7

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.0